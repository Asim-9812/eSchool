

import 'package:flutter/material.dart';

class SubjectPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SUBJECT PAGE',style: TextStyle(color: Colors.black),)
      )
    );
  }
}
